# ML-Projects-
xyz
unzip movie-recommender.zip
cd movie-recommender
git init
git add .
git commit -m "Collaborative filtering movie recommender"
git remote add origin <your-repo-url>
git push -u origin main
